# 功能列表

> 与 [README.md](./README.md) 的简要功能一览表相比，这里是完整的功能清单，按模块整理。

## 1. 登录 / 认证

- 本地模拟登录（工号 ≥4 位 + 密码 ≥6 位即可，非真实账号体系）
- 登录态持久化，未登录自动跳转登录页
- 退出登录

## 2. 打卡（Clock In / Clock Out）

- 拍照打卡，自动叠加水印（姓名、工号、时间、地点、上/下班标签）
- 打卡前请求定位权限，定位失败/拒绝时标记异常但不阻断流程
- 无相机权限时降级为示例占位图，保证流程可跑通
- 同一时段（上/下班）当天重复打卡会被拦截提示
- 支持跨夜班（班次调整后）的"考勤归属日"计算，凌晨打卡正确归到前一天
- 打卡记录列表支持点击照片查看大图

## 3. 考勤记录

- 月历视图，颜色区分：正常出勤 / 迟到（琥珀色）/ 早退（橙色，与迟到独立展示）/ 请假 / 法定节假日 / 缺勤
- 迟到/早退判定按分钟数精确比较（非简单比对分钟位，避免整点打卡漏判）
- 缺勤判定排除法定节假日和排班休息日，不再"非工作日一律算缺勤"
- 迟到/早退判定读取审批通过的班次调整结果，不再写死朝九晚六
- 当天详情：显示应出勤班次、打卡记录（含照片可点击放大）、迟到/早退/加班/请假标签
- 月度统计：应出勤、实际出勤、迟到次数、早退次数、缺勤天数、请假天数、加班时长

## 4. 申请中心（5 类，入口按钮 + 独立子页面）

- **补卡**：限最近 3 天内，时段选择，原因必填，可选附件
- **请假**：菲律宾劳动法假种（VL/SL/SIL/产假/陪产假/单亲假/丧假），可扣减天数自动剔除休息日和法定节假日（非自然日 ceil）
- **加班**：加班类型（工作日/休息日/特殊非工作日/法定节假日）按日期自动识别，只读展示，不再手动选
- **班次调整**：结构化开始/结束时间，审批通过后真实生成生效班次记录，联动考勤计算
- **离职**：预计最后工作日、离职原因、交接说明；明确不校验 DOLE 30 天提前通知规则（由 HR 后台处理）
- 每类申请：提交 → 审批中 → 已通过/已驳回 状态机，进度条可视化
- 审批操作幂等（防止重复点击生成重复数据），仅用于 Demo 演示，标注"正式版本审批在后端完成"
- 补卡审批通过前检查是否已有正常打卡记录，避免同日同时段生成重复记录
- 待审批状态下可撤回申请

## 5. 工资条

- 按半月结（Cutoff：1–15 / 16–月末）展示，含结算周期和发薪日
- 应发明细（基本工资/绩效奖金/加班费）、扣款明细（SSS/PhilHealth/Pag-IBIG/预扣税），菲律宾法定项目命名
- PHP 货币格式
- 查看详情前需通过 Demo 身份校验（模拟，非真实生物识别）
- 醒目的"示例数据，非正式工资条"警示条

## 6. 我的（个人资料）

- 编辑头像（本地图片上传）、显示姓名、性别，改动实时同步到打卡问候语和水印
- 工号/部门只读（现实中属于 HR 系统管理字段）
- 中英文语言切换（默认英文，可切中文）
- 一键重置 Demo 数据

## 7. 国际化

- 完整中英文双语字典，UI 文案、校验提示、状态标签全覆盖
- 假种代码（VL/SL 等）按语言分别展示可读标签

## 8. 移动端体验细节

- 底部导航栏动画聚焦指示条
- 底部弹出面板（Sheet，基于 Vaul）替代所有表单/详情弹窗
- 日期/时间/单列选择器改用 antd-mobile 滚轮选择器，替代原生控件（避免桌面浏览器弹层溢出、语言不跟随问题）
- 页面/卡片进入动效，遵循 `prefers-reduced-motion`
- 法定节假日日历（固定节日 + 复活节相关日期算法计算）

---

已知限制和待办事项见 [BUSINESS_LOGIC_REVIEW.md](./BUSINESS_LOGIC_REVIEW.md)。
